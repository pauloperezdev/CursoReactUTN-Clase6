import React, { useState, Fragment } from "react";

export const Enviar = (props) => {
  return (
    <Fragment>
      <button type="submit" class="btn btn-primary mb-2">
        Enviar
      </button>
    </Fragment>
  );
};

export default Enviar;
